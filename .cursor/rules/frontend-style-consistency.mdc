---
description: 前端样式修改时保持与现有项目风格一致
globs: web/src/**/*.{vue,css,scss,sass,less}
alwaysApply: false
---

# 前端样式一致性

在 `web/src` 下修改或新增样式时，先参考当前页面、同目录组件和相似功能页面的现有实现，保持视觉和代码风格一致。

## 基本原则

- 优先延续现有写法：当前项目以 `Vue` 单文件组件、`Tailwind/原子类`、现有 `Vort` 组件组合为主，非必要不要切换到另一套写法
- 优先最小化修改：只改完成需求所必需的样式，避免顺手重写大段布局、间距、配色或交互态
- 优先复用现有模式：先观察附近页面和相似组件的 class 结构、间距、字号、颜色、圆角、边框、阴影、状态样式，再落地修改

## 具体要求

- 非必要不要新增新的颜色、圆角、阴影、字号、间距尺度；优先复用附近已有值
- 优先复用已有组件和样式习惯，例如 `bg-white`、`rounded-xl`、`rounded-md`、`text-gray-*`、`border-slate-*`、`ring-blue-*` 等现有搭配
- 修改交互元素时，保持与现有按钮、输入框、下拉层、卡片容器一致的视觉层级和反馈方式
- 能复用现有 class 组合时，不新增一套平行命名或单独抽出一组全新风格
- 非必要不要引入内联样式；确实需要动态颜色或运行时值时，再使用 `:style`
- 不要为了“更好看”主动统一全页样式；如果用户没有明确要求，只处理当前需求涉及的区域

## 执行步骤

- 先查看当前文件已有样式写法
- 再查看同目录或相似页面的实现
- 确认可复用的 class、组件和交互模式后再修改
- 如果需求和现有风格冲突，优先贴近现有风格实现，并在回复中说明限制

## 避免事项

- 不要混入与当前页面明显不同的视觉体系
- 不要在小改动中顺带重构整块样式结构
- 不要凭空创造新的命名习惯或设计语言
