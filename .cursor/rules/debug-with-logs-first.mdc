---
description: 前端 bug 排查策略 - 优先使用调试日志而非纯静态分析
alwaysApply: true
---

# 前端 Bug 排查：日志优先原则

排查前端 bug 时，**禁止**长时间通过静态代码分析猜测原因。遵循以下流程：

## 核心原则

**2 分钟规则**：如果阅读代码 2 分钟内无法确定 bug 根因，立即添加调试日志，不要继续猜。

## 排查流程

1. **快速阅读**：花最多 2 轮工具调用理解相关代码
2. **加日志**：在关键路径添加 `console.log` / `console.warn`，包括：
   - 函数入口的参数值
   - 条件分支的判断值
   - API 响应数据
   - 计算属性 / watch 的触发值
   - 事件回调是否被触发
3. **让用户复现**：请用户操作并提供控制台输出
4. **分析日志**：根据运行时数据定位问题
5. **修复后清理**：修复完成后移除所有调试日志

## 日志格式规范

```typescript
// 带标签的日志，方便定位
console.log('[组件名/函数名]', '描述', { key1: val1, key2: val2 })

// 条件分支
console.log('[MyComponent/handleClick]', 'before condition', { status, isReady })

// API 响应
console.log('[useXxx]', 'api response', { data, error })
```

## 禁止行为

- ❌ 连续阅读 5+ 个文件试图"推理"出 bug 原因
- ❌ 在没有运行时证据的情况下反复修改代码"试试看"
- ❌ 修复完成后遗留调试日志

## 关键提醒

静态分析适合理解架构，**不适合排查运行时 bug**。浏览器里实际跑的值才是真相。
