services:
  postgres:
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_USER: openvort
      POSTGRES_PASSWORD: openvort
      POSTGRES_DB: openvort
    ports:
      - "5432:5432"
    volumes:
      - pgdata_dev:/var/lib/postgresql/data

volumes:
  pgdata_dev:
