services:
  openvort:
    build: .
    ports:
      - "10899:8090"
    env_file: .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      PYTHONUNBUFFERED: "1"
    volumes:
      - openvort_data:/root/.openvort
    restart: unless-stopped

volumes:
  openvort_data:
